<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianqi.auth.dao.ITqAuthResourceDAO">
    <resultMap type="com.tianqi.auth.pojo.TqAuthResourceDO" id="TqAuthResourceMap">
        <result property="appId" column="app_id" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="columns">
        <include refid="com.tianqi.common.dao.IBaseDAO.commonCol"/>
        app_id

    </sql>
    <select id="selectMenuResource" resultType="com.tianqi.auth.pojo.dto.resp.ResourceDetailDTO">
        select t.id , t.name,t.parent_id,t.type , t.url ,t.component,t.closeable , t.icon from tq_auth_resource t left
        join tq_auth_role_resource_relation a on t.id = a.resource_id
        left join tq_auth_role b on a.role_id = b.id and b.tenant_id = #{tenantId} and b.code in
        <foreach collection="roles" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
        where t.type = 'M' and t.app_id = #{appId}
    </select>


</mapper>

